import React from "react";
import { TrendingUp, Users, Building2, Award } from "lucide-react";
import { motion } from "framer-motion";

export default function StatsSection({ vacanciesCount, internshipsCount }) {
  const stats = [
    {
      icon: Building2,
      value: "50+",
      label: "Компаний-резидентов",
      color: "from-blue-500 to-cyan-500"
    },
    {
      icon: TrendingUp,
      value: vacanciesCount,
      label: "Активных вакансий",
      color: "from-indigo-500 to-purple-500"
    },
    {
      icon: Users,
      value: internshipsCount,
      label: "Мест стажировки",
      color: "from-purple-500 to-pink-500"
    },
    {
      icon: Award,
      value: "1000+",
      label: "Успешных трудоустройств",
      color: "from-orange-500 to-red-500"
    }
  ];

  return (
    <section className="py-12 -mt-12 relative z-10">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="grid grid-cols-2 lg:grid-cols-4 gap-6">
          {stats.map((stat, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: index * 0.1 }}
              className="bg-white rounded-2xl p-6 shadow-xl hover:shadow-2xl transition-all duration-300"
            >
              <div className={`w-12 h-12 bg-gradient-to-r ${stat.color} rounded-xl flex items-center justify-center mb-4`}>
                <stat.icon className="w-6 h-6 text-white" />
              </div>
              <div className="text-3xl font-bold text-gray-900 mb-1">
                {stat.value}
              </div>
              <div className="text-sm text-gray-500">
                {stat.label}
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}